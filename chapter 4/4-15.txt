啥也返回不出来
这就是图灵的停机问题，
有点像哥德尔定理，都是靠悖论，这次的悖论是这样的

假设存在这么一个halt，它可以针对任何过程都给出结果，要嘛有返回值，要嘛永远没有
也就是halt(p,i)给出结论，要嘛永远不停，要嘛总会给出个结果

因为程序本身也是数据，所以halt(p,p)也应该能给出正确结果

那么，我们定义下面一个过程

那么，输入下面这个过程
这个过程描述如下：
判断自身是否停机，如果有返回值，那么无限空循环，永远循环下去
如果停不下来，那就返回一个有效值

(define (k p)
	(if (halt? p p)
		永久循环
		返回有效值))

听起来很分裂

然后再用(halt? k k)分析
如果返回永久循环，那么根据定义，(halt? k k)是有返回值的，矛盾
反之也一样

所以说，当初的假设是错误的，这种halt?是不存在的，是不可计算过程
所以这种halt?根本不存在